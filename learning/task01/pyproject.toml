[project]
name = "research_deepagent"
version = "0.1.0"
description = "Research DeepAgent — DeepAgents research agent with Tavily search, served by AgentSeek API."
authors = [{ name = "Your Name" }]
requires-python = ">=3.12"
dependencies = [
    "deepagents>=0.5.3",
    "langchain>=1.0",
    "langchain-anthropic>=1.0",
    "langchain-google-genai>=4.0",
    "langchain-openai>=0.3",
    "tavily-python>=0.5",
    "httpx[socks]>=0.28",
    "markdownify>=0.13",
    "python-dotenv>=1.0",
    "agentseek-api[embedded]==0.2.3",
    "mcp>=1.27.1,<2",
]

[build-system]
requires = ["setuptools>=69"]
build-backend = "setuptools.build_meta"

[tool.setuptools]
package-dir = {"" = "src"}

[tool.setuptools.packages.find]
where = ["src"]
